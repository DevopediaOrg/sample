name: Example

on:
  push:
    branches: [ "dev", "rel-*" ]
    tags: [ "v[0-9]+.[0-9]+.[0-9]+" ]

jobs:

  build:

    runs-on: ubuntu-20.04
    environment: default
    timeout-minutes: 10

    steps:

      - name: Inspect environment
        run: |
          echo "----------------------------------------------------------"
          echo Event: ${{ github.event_name }}
          echo Branch: ${{ github.ref }}
          echo Repo: ${{ github.repository }}
          echo Commit ID: ${{ github.sha }}
          echo "----------------------------------------------------------"
          echo Home dir: $HOME
          echo Current dir: $(pwd)
          echo "----------------------------------------------------------"
          uname -a
          lsb_release -a
          echo "----------------------------------------------------------"

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Get tag name
        uses: olegtarasov/get-tag@v2.1.2
        with:
          tagRegex: "v[0-9]+\\.[0-9]+\\.[0-9]+\\"

      - name: Set new env variables
        run: |
          echo COMMIT_ID=$(git rev-parse --short $GITHUB_SHA) >> $GITHUB_ENV
          echo CURR_BR=${GITHUB_REF#refs/heads/} >> $GITHUB_ENV

      - name: Access env variables
        run: |
          echo $GIT_TAG_NAME
          echo $COMMIT_ID
          echo $CURR_BR
          envsubst < .env
          sleep 5s

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Pushing EMS Docker image to Docker Hub
        run: |
          docker search --format "{{.Name}}" resonous003
          sleep 5s

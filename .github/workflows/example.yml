name: Example

on:
  push:
    branches: [ "dev", "rel-*" ]

jobs:

  build:

    runs-on: ubuntu-20.04
    timeout-minutes: 10

    steps:

      - name: Inspect environment
        run: |
          echo "----------------------------------------------------------"
          echo Event: ${{ github.event_name }}
          echo Branch: ${{ github.ref }}
          echo Repo: ${{ github.repository }}
          echo Commit ID: ${{ github.sha }}
          echo "----------------------------------------------------------"
          echo Home dir: $HOME
          echo Current dir: $(pwd)
          echo "----------------------------------------------------------"
          uname -a
          lsb_release -a
          echo "----------------------------------------------------------"

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set new env variables
        run: |
          echo commmitid=$(git rev-parse --short $GITHUB_SHA) >> $GITHUB_ENV
          echo currbr=${GITHUB_REF#refs/heads/} >> $GITHUB_ENV

      - name: Access env variables
        run: |
          echo $commitid
          echo $currbr

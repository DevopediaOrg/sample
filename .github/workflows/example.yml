name: Example

on:
  push:
    branches: [ "dev", "rel-*" ]

jobs:

  build:

    runs-on: ubuntu-20.04
    environment: default
    timeout-minutes: 10

    steps:

      - name: Warm up
        run: sleep 5s

      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Inspect environment
        run: |
          echo "----------------------------------------------------------"
          echo Event: ${{ github.event_name }}
          echo Branch: ${{ github.ref }}
          echo Repo: ${{ github.repository }}
          echo Commit ID: ${{ github.sha }}
          echo "----------------------------------------------------------"
          echo Home dir: $HOME
          echo Current dir: $(pwd)
          echo "----------------------------------------------------------"
          uname -a
          lsb_release -a
          echo "----------------------------------------------------------"

      - name: Get tag name
        run: |
          git fetch -q --tags origin
          TAGNAME=$(git describe --always --tags ${{ github.sha }})
          if [ -z "$TAGNAME" ]; then echo "No tag"; else echo "Tagname: $TAGNAME"; fi

      - name: Set new env variables
        run: |
          echo COMMIT_ID=$(git rev-parse --short $GITHUB_SHA) >> $GITHUB_ENV
          echo CURR_BR=${GITHUB_REF#refs/heads/} >> $GITHUB_ENV

      - name: Access env variables
        run: |
          echo $COMMIT_ID
          echo $CURR_BR
